apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend
  namespace: production
  labels:
    app: backend
  annotations:
    # IRSA annotation for AWS (update with your account ID and role name)
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/infragpt-backend
automountServiceAccountToken: true
